{
  "name": "won-top-buildinghub-relay",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "types": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260820.1",
    "@types/node": "^26.2.0",
    "wrangler": "^4.124.0"
  }
}
